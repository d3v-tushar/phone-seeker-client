import React from 'react';
import { useTitle } from '../../../CustomHook/useTitle';

const Dashboard = () => {
    useTitle('Dashboard - ')
    return (
        <div>
            <h2>Comming Soon</h2>
        </div>
    );
};

export default Dashboard;